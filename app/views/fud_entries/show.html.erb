<div class="panel panel-default">
  <div class="panel-heading">
    <div class="panel-title">
      <h1>Ohhh that's good FUD :)</h1>
    </div>
  </div>
  <div class="panel-body">
  <strong>FUD:</strong>
  <blockquote><%= @fud_entry.fud %></blockquote>

  
<% if @fud_entry.anonymous %>
  	<strong><em>Mum's the word</em> ;) FUD submitted anonymously!</strong>
<% else %>
	<strong>Your FUD <u>was not</u> posted anonymously. Edit it if you'd like to be anonymous.</strong>
<% end %>
    
  </div>
  <div class="panel-footer">
	<% if !@fud_entry.anonymous %>
		<%= link_to 'Edit', edit_fud_entry_path(@fud_entry), :class => 'btn btn-primary btn-xs' %> 
	<% end %>
	<%= link_to 'Back', fud_entries_path, :class => 'btn btn-success btn-xs' %>
  </div>
</div>