<%= form_for(@fud_entry) do |f| %>
  <% if @fud_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fud_entry.errors.count, "error") %> prohibited this fud_entry from being saved:</h2>

      <ul>
      <% @fud_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :fud %><br>
    <%= f.text_area :fud, placeholder: 'Type your FUD here...', :id => "message_area", :cols => 50, :rows => 4 %>
  </div>
  <div class="field">
   <%= f.label :'We value transparency and encourage you to freely share your comments.' %>.<br>If necessary, check here to post anonymously&nbsp;
    <%= f.check_box :anonymous, {:checked=>false} %><br><br>
  </div>
  
    <%= f.submit 'Submit FUD', :class => 'btn btn-primary' %>
  

<% end %>
